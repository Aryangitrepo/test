name: test

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: installing 
        run: sudo apt install -y npm && apt install -y nodejs
      - name: starting 
        run: npm test 
